/**
 * This enum defines the rounding mode to use when rounding [[Money]].
 */
export enum RoundingMode {
  /**
   * Round up to the next step value towards positive infinity. If the current
   * value is already on a step value, it will not be rounded.
   */
  UP,

  /**
   * Round down to the next step value towards negative infinity. If the current
   * value is already on a step value, it will not be rounded.
   */
  DOWN,

  /**
   * Round to the nearest step value. If the current value is _exactly_ between
   * step values, it will round to the step value away from zero.
   */
  NEAREST,
}
