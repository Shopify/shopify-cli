export class Key {
  public name: string;

  @shopify_transient
  private transient: string;

  constructor(name: string, transient: string) {
    this.name = name;
    this.transient = transient;
  }
}

export class Value {
  constructor(public name: string) {}
}

export type MyMap = Map<string, string>;

export const enum Flag {
  A,
  B,
  C,
}
