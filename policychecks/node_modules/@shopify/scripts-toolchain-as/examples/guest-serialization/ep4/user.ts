export class User {
  // field renaming
  @shopify_rename('foo')
  bar: string;

  // foo: string; // error for having existed field name 'foo'

  @shopify_transient
  baz: string;

  constructor(bar: string) {
    this.bar = bar;
  }
}

export type Quantity = u64;
