query AllOrgs{
  organizations(first: 3) {
    nodes {
      id
      businessName
      website
      stores(first: 50) {
        nodes {
          shopId
          link
          shopDomain
          shopName
          transferDisabled
          convertableToPartnerTest
        }
      }
    }
  }
}
