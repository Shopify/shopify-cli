query FindOrg($id: ID!) {
  organizations(id: $id, first: 1) {
    nodes {
      id
      businessName
      website
      stores(first: 190) {
        nodes {
          link
          shopDomain
          shopName
          transferDisabled
          convertableToPartnerTest
        }
      }
    }
  }
}
