query FindOrg($id: ID!) {
  organizations(id: $id) {
    nodes {
      id
      businessName
      website
      stores {
        nodes {
          link
          shopDomain
          shopName
        }
      }
    }
  }
}

