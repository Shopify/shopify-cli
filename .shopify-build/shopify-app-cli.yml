containers:
  default:
    docker: circleci/ruby:2.5.1-node-browsers

steps:
- label: Lint
  timeout: 5m
  run:
    - bundle: ~
    - bundle exec rubocop

- label: Tests
  timeout: 5m
  run:
    - bundle: ~
    - bundle exec rake test
